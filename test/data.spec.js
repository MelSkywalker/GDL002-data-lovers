require('../src/data.js');
const pokemon = require('../src/data/pokemon/pokemon.json');



// describe('example', () => {
//   it('is a function', () => {
//     expect(typeof example).toBe('function');
//   });

//   it('returns `example`', () => {
//     expect(example()).toBe('example');
//   });
// });

describe('calculateMaxCP()', () => {
  it('debería ser una función', () => {
    expect(typeof data.calculateMaxCP).toBe("function");
  });

  it('debería retornar 160 para Bulbasaur con CP de 101', () => {
    expect().toBe();
  });
});